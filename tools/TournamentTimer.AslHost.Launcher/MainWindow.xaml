<Window x:Class="TournamentTimer.AslHost.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TournamentTimer ASL Host Launcher"
        Width="760"
        Height="560"
        MinWidth="660"
        MinHeight="460"
        Background="#0B0D12"
        Foreground="#F4F4F5"
        FontFamily="Segoe UI"
        Icon="assets/branding/tournament-timer.ico">

	<Window.Resources>
		<SolidColorBrush x:Key="PageBackground" Color="#0B0D12"/>
		<SolidColorBrush x:Key="CardBackground" Color="#151923"/>
		<SolidColorBrush x:Key="InputBackground" Color="#090B10"/>
		<SolidColorBrush x:Key="BorderColor" Color="#2A3040"/>
		<SolidColorBrush x:Key="TextColor" Color="#F4F4F5"/>
		<SolidColorBrush x:Key="MutedTextColor" Color="#AAB3C5"/>
		<SolidColorBrush x:Key="PrimaryColor" Color="#2F6BFF"/>
		<SolidColorBrush x:Key="DangerColor" Color="#A83232"/>
		<SolidColorBrush x:Key="SecondaryColor" Color="#2A3040"/>

		<Style x:Key="ProjectButtonStyle" TargetType="{x:Type Button}">
			<Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
			<Setter Property="Foreground" Value="{StaticResource TextColor}"/>
			<Setter Property="BorderBrush" Value="#364052"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Padding" Value="14,8"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
						</Border>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="ButtonBorder" Property="Opacity" Value="0.75"/>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42"/>
								<Setter Property="Cursor" Value="Arrow"/>
								<Setter Property="Foreground" Value="#B3BACB"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="PrimaryButtonStyle"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource ProjectButtonStyle}">
			<Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
			<Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
		</Style>

		<Style x:Key="DangerButtonStyle"
               TargetType="{x:Type Button}"
               BasedOn="{StaticResource ProjectButtonStyle}">
			<Setter Property="Background" Value="{StaticResource DangerColor}"/>
			<Setter Property="BorderBrush" Value="{StaticResource DangerColor}"/>
		</Style>

		<Style TargetType="{x:Type TextBox}">
			<Setter Property="Background" Value="{StaticResource InputBackground}"/>
			<Setter Property="Foreground" Value="{StaticResource TextColor}"/>
			<Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CaretBrush" Value="{StaticResource TextColor}"/>
			<Setter Property="SelectionBrush" Value="{StaticResource PrimaryColor}"/>
			<Setter Property="Padding" Value="12,7"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
		</Style>

		<Style TargetType="{x:Type RadioButton}">
			<Setter Property="Foreground" Value="{StaticResource TextColor}"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Padding" Value="14,8"/>
			<Setter Property="MinWidth" Value="64"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type RadioButton}">
						<Border x:Name="RadioBorder"
                                Background="#0F121A"
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
						</Border>

						<ControlTemplate.Triggers>
							<Trigger Property="IsChecked" Value="True">
								<Setter TargetName="RadioBorder" Property="Background" Value="{StaticResource PrimaryColor}"/>
								<Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
								<Setter Property="Foreground" Value="#FFFFFF"/>
							</Trigger>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="RadioBorder" Property="Opacity" Value="0.9"/>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="RadioBorder" Property="Opacity" Value="0.45"/>
								<Setter Property="Cursor" Value="Arrow"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="{x:Type Thumb}">
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Thumb}">
						<Border Background="#343C51"
								BorderBrush="#475069"
								BorderThickness="1"
								CornerRadius="6"/>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="{x:Type ScrollBar}">
			<Setter Property="Background" Value="{StaticResource InputBackground}"/>
			<Setter Property="Width" Value="12"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ScrollBar}">
						<Grid Background="{TemplateBinding Background}">
							<Track x:Name="PART_Track"
								   IsDirectionReversed="True">
								<Track.DecreaseRepeatButton>
									<RepeatButton Command="{x:Static ScrollBar.LineUpCommand}"
												  Opacity="0"
												  IsTabStop="False"/>
								</Track.DecreaseRepeatButton>

								<Track.Thumb>
									<Thumb Margin="2"/>
								</Track.Thumb>

								<Track.IncreaseRepeatButton>
									<RepeatButton Command="{x:Static ScrollBar.LineDownCommand}"
												  Opacity="0"
												  IsTabStop="False"/>
								</Track.IncreaseRepeatButton>
							</Track>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		
	</Window.Resources>

	<Grid Margin="22">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0"
                    Margin="0,0,0,18">
			<TextBlock Text="ASL Host Launcher"
                       FontSize="30"
                       FontWeight="Black"
                       Foreground="{StaticResource TextColor}"/>

			<TextBlock Text="External ASL Host control for Runner UI LiveSplit bridge"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource MutedTextColor}"
                       Margin="1,4,0,0"/>
		</StackPanel>

		<Border Grid.Row="1"
                Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="16"
                Margin="0,0,0,14">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<TextBlock Grid.Column="0"
                           Text="RunId"
                           VerticalAlignment="Center"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="{StaticResource MutedTextColor}"
                           Margin="0,0,14,0"/>

				<TextBox x:Name="RunIdTextBox"
                         Grid.Column="1"
                         Text="local-test-run"
                         Height="38"
                         FontWeight="SemiBold"/>

				<StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            Margin="16,0,0,0"
                            VerticalAlignment="Center">
					<RadioButton x:Name="X64RadioButton"
                                 Content="x64"
                                 GroupName="Architecture"
                                 IsChecked="True"
                                 Margin="0,0,8,0"/>

					<RadioButton x:Name="X86RadioButton"
                                 Content="x86"
                                 GroupName="Architecture"/>
				</StackPanel>
			</Grid>
		</Border>

		<Border Grid.Row="2"
                Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="16"
                Margin="0,0,0,14">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<StackPanel Grid.Column="0">
					<TextBlock Text="Status"
                               Foreground="{StaticResource MutedTextColor}"
                               FontSize="12"
                               FontWeight="Bold"
                               Margin="0,0,0,5"/>

					<TextBlock x:Name="StatusTextBlock"
                               Text="Stopped"
                               FontSize="24"
                               FontWeight="Black"
                               Foreground="{StaticResource TextColor}"/>
				</StackPanel>

				<StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
					<Button x:Name="StartButton"
                            Content="Start"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="96"
                            Height="40"
                            Margin="0,0,8,0"/>

					<Button x:Name="StopButton"
                            Content="Stop"
                            Style="{StaticResource DangerButtonStyle}"
                            Width="96"
                            Height="40"
                            IsEnabled="False"
                            Margin="0,0,8,0"/>

					<Button x:Name="RestartButton"
                            Content="Restart"
                            Style="{StaticResource ProjectButtonStyle}"
                            Width="104"
                            Height="40"
                            IsEnabled="False"/>
				</StackPanel>
			</Grid>
		</Border>

		<Border Grid.Row="3"
                Background="{StaticResource InputBackground}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="12">
			<TextBox x:Name="LogTextBox"
                     Text="Ready."
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="Transparent"
                     Foreground="#D7DBE7"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="0"/>
		</Border>
	</Grid>
</Window>